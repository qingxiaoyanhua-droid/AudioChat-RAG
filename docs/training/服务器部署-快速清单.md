# 服务器部署 - 快速命令清单

## 📦 1. 本地打包（Windows）

```bash
# 在项目根目录执行
cd d:\VscodeProject\Users\setupmac\DiTX-Clerk

# 使用 7-Zip 或 WinRAR 压缩为 DiTX-Clerk.zip
# 或使用 git
git archive -o DiTX-Clerk.tar.gz HEAD
```

---

## 📤 2. 上传到服务器

```bash
# PowerShell 执行
scp DiTX-Clerk.tar.gz 你的用户名@10.20.126.25:/data/
```

**或使用 WinSCP/FileZilla:**
- 主机：10.20.126.25
- 用户名：你的用户名
- 密码：你设置的密码
- 上传路径：`/data/DiTX-Clerk/`

---

## 🔑 3. 登录服务器

```bash
# 首次登录管理员账户创建用户
ssh vpdlab@10.20.126.25
# 密码：VPDserver123

# 创建个人账户
sudo adduser 你的用户名
# 密码：VPDserver123，然后设置自己的密码

# 退出管理员账户
exit

# 登录个人账户
ssh 你的用户名@10.20.126.25
```

---

## 📥 4. 解压项目

```bash
cd /data
mkdir -p DiTX-Clerk
cd DiTX-Clerk

# 解压
tar -xzf /data/DiTX-Clerk.tar.gz -C . --strip-components=1
```

---

## ⚙️ 5. 部署环境

```bash
# 激活环境
source /home/ditx/llm/voice/3D-Speaker/.3ds/bin/activate

# 或直接使用 Python 路径
PYTHON="/home/ditx/llm/voice/3D-Speaker/.3ds/bin/python3"

# 运行部署脚本
chmod +x deploy.sh
./deploy.sh
```

---

## 🚀 6. 快速测试

```bash
# 生成数据
$PYTHON prepare_data.py --output_dir data --num_sft 50 --num_grpo 50 --num_rag 10

# 搭建 RAG
$PYTHON setup_rag_db.py --storage_dir ./rag_storage --data_dir data/meeting_records

# 运行演示
$PYTHON demo_quick.py
```

---

## 📊 7. 完整训练流程

```bash
# === 1. 准备数据 ===
$PYTHON prepare_data.py --output_dir data --num_sft 1000 --num_grpo 1000

# === 2. 搭建 RAG ===
$PYTHON setup_rag_db.py --storage_dir ./rag_storage --data_dir data/meeting_records

# === 3. SFT 训练（8 卡）===
torchrun --nproc_per_node=8 train_sft.py \
  --model_name_or_path /data/models/Voice/FunAudioLLM/Fun-Audio-Chat-8B \
  --train_data data/sft_train_data.jsonl \
  --output_dir saves/sft_model \
  --num_train_epochs 3

# === 4. GRPO 训练（4 卡）===
torchrun --nproc_per_node=4 train_grpo.py \
  --model_name_or_path saves/sft_model \
  --train_data data/grpo_train_data.jsonl \
  --output_dir saves/grpo_model \
  --num_train_epochs 3
```

---

## 🔍 8. 常用检查命令

```bash
# 检查 GPU
nvidia-smi

# 实时监控 GPU
watch -n 1 nvidia-smi

# 检查 Python
$PYTHON --version

# 检查依赖
$PYTHON -c "import torch; import transformers; print('OK')"

# 检查模型
ls /data/models/Voice/FunAudioLLM/Fun-Audio-Chat-8B

# 查看训练进度
tail -f saves/sft_model/trainer_log.txt
```

---

## ⚠️ 注意事项

1. **不要升级系统**: `sudo apt upgrade` ❌
2. **数据放 `/data/`**: 所有数据放在 `/data/` 目录下
3. **不要登录管理员账户**: 除非创建新用户
4. **SSH 问题**: `ssh-keygen -R 10.20.126.25`

---

## 📞 服务器信息

| 项目 | 值 |
|------|-----|
| 服务器地址 | 10.20.126.25 |
| 管理员账户 | vpdlab |
| 管理员密码 | VPDserver123 |
| Python 路径 | /home/ditx/llm/voice/3D-Speaker/.3ds/bin/python3 |
| 模型目录 | /data/models/Voice/ |
| 数据目录 | /data/ |

---

**预计时间**: 部署 10 分钟 + 训练 7-10 小时
