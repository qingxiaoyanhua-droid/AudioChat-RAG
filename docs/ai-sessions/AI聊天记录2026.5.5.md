# 会话总结 — 2026.05.05 Cursor 对话

> 用途：让下一个 AI 快速了解本轮对话的核心信息。
> 整理自 Cursor AI Coding Assistant 对话记录。

---

## 一、今日主要工作

### 1. 服务器部署尝试

**目标**：将本地 DiTX-Clerk 代码上传到公司服务器。

**尝试 1：scp 直接上传**
```powershell
scp -r -P 14136 "d:\VscodeProject\Users\setupmac\DiTX-Clerk" root@js4.blockelite.cn:/root/DiTX-Clerk
# 结果：Connection closed by 198.18.0.48 port 14136
```
- **原因**：SSH key 未加入服务器 authorized_keys，或端口/认证有问题
- **结论**：搁置

**备选方案**：
1. 先上传 GitHub，服务器 `git clone`
2. 用学校服务器（内网可连）+ 本地开发机协作

**最终选择**：暂时搁置部署，优先整理文档记录。学校服务器方案待后续实现。

### 2. 文档整理

将本轮对话中产生的所有讨论整合进现有文档体系：

- `PROJECT_WORKLOG.md`：增加第十节（今日会话）+ 第十一节（面试反馈复盘）
- 核心更新内容：
  - 面试反馈整理（WXG 一二面、百度一面关键追问）
  - 技术知识补强清单（BGE/HNSW/BM25/ChromaDB 等）
  - 诚实版实验话术精炼版

---

## 二、本轮讨论的技术内容

### 2.1 软 Gate 机制（最终版）

**最终结论**：用 `structure_score ** 0.5` 对下游维度做比例缩放，替代硬截断。

```python
downstream_weight = structure_score ** 0.5
faithfulness = self._faithfulness_reward(generated, retrieved_docs) * downstream_weight
dep          = self._dependency_validity_reward(action_items) * downstream_weight
exec_        = self._task_executability(action_items) * downstream_weight
```

**为什么用 √s 而不是 s**：√s 是凸函数，对低分压缩更狠（s=0.1 → √s=0.32），高分端温和释放（s=0.9 → √s=0.95）。

**加保险**：KL 自适应安全因子，防止 policy 漂移时下游 reward 剧变。

### 2.2 面试反馈核心洞察

#### WXG 二面最重要的反馈：串行 Reward 设计

面试官追问："结构化失败时，忠实度/DAG/executability 还在对'废输出'打分，reward signal 被污染。"

这直接引出了从"硬 gate"到"软 gate"的设计演进。

#### 简历问题（最紧急）

面试官明确说："第一点'语音流水线'不太适合。" — ASR 部分不是你做的，只是调用函数。

---

## 三、项目当前状态

### 已完成

- [x] 软 Gate 机制实现（coherence reward + soft gate）
- [x] 9 维度奖励函数（准确/忠实/流畅/相关/结构/长度/dep/exec/coherence）
- [x] 文档整理（WORKLOG + CONTEXT 同步更新）
- [x] 面试反馈系统整理（WXG 一二面 + 百度一面）

### 待处理

- [ ] BGE v1.5 vs v1 差异补强（去了解）
- [ ] HNSW 召回率/复杂度补强
- [ ] BM25 IDF 公式补强
- [ ] ChromaDB vs Faiss 区别澄清
- [ ] OpenCompass 四层架构整理
- [ ] ASR 评价指标（WER/RTF/CER/SER）补强
- [ ] 简历修改（删除"语音流水线"描述）

### 服务器部署（暂停）

- 原因：SSH 连接无法建立
- 替代方案待定（GitHub 上传 / 学校服务器）

---

## 四、面试必背更新

新增面试反馈追问链：

```
Q: 你的 reward 维度是串行还是并行？
A: 是带条件权重的并行 reward — 下游维度 = 原始 reward × √(structure_score)
   结构化越差，下游维度贡献越低，但不会归零（连续可导，无 reward cliff）
```

---

*整理自 2026.05.05 Cursor 对话记录*
